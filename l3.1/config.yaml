HTTP:
  ADDRESS: "localhost:8080"

POSTGRES:
  POSTGRES_USER: user
  POSTGRES_PASSWORD: user
  POSTGRES_HOST: localhost
  POSTGRES_PORT: 5432
  POSTGRES_DB: mydb
  MAX_CONNS: 10
  MIN_CONNS: 5
  POSTGRES_DSN: postgres://user:user@localhost:5432/DelayedNotifier,